# htmdec_models


TODOs:

### Birdshot

- define naming convention with graphml file AND use a Girder uploader to push them back to a folder on the Birdshot collection
- define order of operation via a schema, or possibly use sample tracker to know the order of operation (and linking between the different teams)
- define and add metadata on each run that can go on GEMD, but also girder for the graph, like what team is missing, what file is missing, if something failed, etc

- needs revision with the help of each team